# SIGEI

## Project Stack

- **For Backend:** Java 17, Intellij Idea, SpringBoot Reactive

- **For Frontend:** React and TailwindCss 4.

- **For database:** MongoDB and PostgreSQL

## ✅ Project Purpose

The SIGEI project is a project that seeks to optimize and improve management in early childhood education schools.

## 🛠️ Setup Instructions (Imperatives)

1. **Clone** the repository:

```bash
git clone https://github.com/MariaSulcaDev/vg-ms-users.git
```

2. **Navigate** into backend:

```bash
cd vg-ms-users/
```

3. **Run** Spring Boot app:

```bash
mvn spring-boot:run
```

4. **Navigate** into frontend:

```bash
cd vg-web-sigei/
```

5. **Install** dependencies and **run** the React app:

```bash
npm run install
npm run dev
```


